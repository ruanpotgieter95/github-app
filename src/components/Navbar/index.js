import React from 'react';
import './styles.scss';

export default () => {
    return (
        <nav>
            <span>Logo</span>
            <span className="hover">Logout</span>
        </nav>
    )
}

